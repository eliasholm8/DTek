Instructions for linux

to compile and run the program, make sure to have the mcb32tools toolchan installed. then run:

. /opt/mcb32tools/environment

to build the program, run make
to to flash the CipKIT, run make install

Your C compiler must support the C11 standard for the program to compile.